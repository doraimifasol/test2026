# Tetris 游戏说明文档

## 概述

本项目包含一个用 C 语言编写的终端俄罗斯方块（Tetris）小游戏，文件名为 `tetris.c`。

游戏在终端中以文本方式显示，并支持键盘输入来控制方块移动、旋转、下落以及退出游戏。

## 文件结构

- `tetris.c`：游戏主程序源代码。
- `tetris_doc.md`：本说明文档。

## 主要组件说明

### 头文件

- `stdio.h`：用于输入输出函数，例如 `printf`。
- `stdlib.h`：用于动态内存、随机数、退出程序等函数。
- `string.h`：用于内存拷贝和清零操作，如 `memcpy`、`memset`。
- `termios.h`：用于设置终端的原始模式，允许实时读取按键。
- `unistd.h`：用于读取标准输入、休眠等系统调用。
- `sys/select.h`：用于 `select` 函数检测键盘输入。
- `sys/time.h`：用于获取系统时间。
- `time.h`：用于随机数种子初始化。

### 常量定义

- `BOARD_WIDTH`：游戏板宽度，默认 10。
- `BOARD_HEIGHT`：游戏板高度，默认 20。
- `PIECE_SIZE`：方块矩阵大小，默认 4。

### 全局变量

- `orig_termios`：保存终端原始设置，用于游戏结束后恢复。
- `board`：二维数组，表示游戏板格子状态。
- `piece_type`：当前方块类型（0~6）。
- `piece_rotation`：当前方块旋转状态（0~3）。
- `piece_x`、`piece_y`：当前方块在游戏板中的位置。
- `score`：游戏得分。

### 方块数据

变量 `pieces` 包含 7 种标准俄罗斯方块类型（I、J、L、O、S、T、Z），每种类型包含 4 种旋转状态。

## 函数说明

### 终端控制函数

- `disable_raw_mode`：恢复终端设置为原始状态。
- `enable_raw_mode`：设置终端为原始模式，禁用回显和行缓冲，使游戏可即时响应按键。

### 输入处理函数

- `kbhit`：检测是否有键盘输入可读。
- `read_char`：读取单个字符，如果没有读取到字符则返回 -1。

### 游戏逻辑函数

- `collision(int nx, int ny, int rotation)`：检测当前方块在指定位置和旋转下是否发生碰撞。返回 1 表示碰撞，0 表示无碰撞。
- `place_piece`：将当前方块固定到游戏板上。
- `clear_lines`：检查并消除已填满的行，同时增加得分。
- `spawn_piece`：随机生成一个新方块，并重置位置；若初始位置即发生碰撞，则游戏结束。
- `draw`：清屏并绘制当前游戏状态，包括游戏板、当前方块和分数。使用 ANSI 转义序列实现终端刷新。

### 主循环

`main` 函数执行以下主要流程：

1. 初始化随机数种子。
2. 启用终端原始模式。
3. 清空游戏板并生成第一个方块。
4. 进入游戏循环：
   - 检查键盘输入并执行移动、旋转、快速下落或退出。
   - 定时更新方块自动下落。
   - 方块无法继续下落时固定方块、消行、生成新方块。
   - 每次状态变化后重新绘制游戏界面。
5. 在退出前恢复终端设置并输出最终得分。

## 操作说明

- `a` 或左箭头：向左移动方块。
- `d` 或右箭头：向右移动方块。
- `s` 或下箭头：向下移动方块。
- `w`：旋转方块。
- `空格`：立即下落到最下方。
- `q`：退出游戏。

## 编译与运行

使用以下命令进行编译和运行：

```bash
gcc -o tetris tetris.c
./tetris
```

## 注意事项

- 该游戏仅在支持 ANSI 终端控制字符的终端中正常显示。
- 目前代码没有实现更高级的功能，例如方块预览、下一块显示、等级加速、消除多行奖励等。
- 该程序适合作为学习俄罗斯方块逻辑与终端控制的示例。
